package com.beautysalonapp.modules.finance.domain;

/** Çek/senet türü (§10.7). */
public enum ChequeType {
    MUSTERI_CEKI,  // alınan (müşteriden)
    FIRMA_CEKI     // verilen (firma çeki/senedi)
}
