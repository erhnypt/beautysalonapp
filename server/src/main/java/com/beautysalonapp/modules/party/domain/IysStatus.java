package com.beautysalonapp.modules.party.domain;

/** İYS (İleti Yönetim Sistemi) izin durumu (§8.3). Ticari ileti yalnızca IZINLI'ye gider. */
public enum IysStatus {
    IZINLI,
    IZINSIZ,
    BILINMIYOR
}
