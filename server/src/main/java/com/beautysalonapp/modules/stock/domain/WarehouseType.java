package com.beautysalonapp.modules.stock.domain;

/** Depo türü (§9.3). SHOWCASE=vitrin, WAREHOUSE=depo, CONSUMPTION=sarf (hizmet tüketimi). */
public enum WarehouseType {
    SHOWCASE,
    WAREHOUSE,
    CONSUMPTION
}
